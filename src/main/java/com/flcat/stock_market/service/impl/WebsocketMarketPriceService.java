//package com.flcat.stock_market.service.impl;
//
//import com.flcat.stock_market.dto.StockPriceResponseDto;
//import lombok.extern.slf4j.Slf4j;
//import org.json.JSONException;
//import org.springframework.stereotype.Service;
//
//import java.io.IOException;
//import java.util.*;
//
//@Slf4j
//@Service
//public class WebsocketMarketPriceService {
//

/*
        JSONParser parser = new JSONParser();
        Object object;
        JSONObject jsonObject = new JSONObject();
        JSONObject header = (JSONObject) jsonObject.get("header");
        if ()
                JSONObject outputs = jsonObject.getJSONObject("output");
                stockPriceResponseDto.setRsym(outputs.get("rsym").toString());
                stockPriceResponseDto.setSymb(outputs.get("symb").toString());
                stockPriceResponseDto.setZdiv(outputs.get("zdiv").toString());
                stockPriceResponseDto.setXymd(outputs.get("xymd").toString());
                stockPriceResponseDto.setXhms(outputs.get("xhms").toString());
                stockPriceResponseDto.setKymd(outputs.get("kymd").toString());
                stockPriceResponseDto.setKhms(outputs.get("khms").toString());
                stockPriceResponseDto.setBvol(outputs.get("bvol").toString());
                stockPriceResponseDto.setAvol(outputs.get("avol").toString());
                stockPriceResponseDto.setBdvl(outputs.get("bdvl").toString());
                stockPriceResponseDto.setAdvl(outputs.get("advl").toString());
                stockPriceResponseDto.setPbid1(outputs.get("pbid1").toString());
                stockPriceResponseDto.setPask1(outputs.get("pask1").toString());
                stockPriceResponseDto.setVbid1(outputs.get("vbid1").toString());
                stockPriceResponseDto.setVask1(outputs.get("vask1").toString());
                stockPriceResponseDto.setDbid1(outputs.get("dbid1").toString());
                stockPriceResponseDto.setDask1(outputs.get("dask1").toString());
                List<StockPriceResponseDto> dtos = new ArrayList<>();
                dtos.add(stockPriceResponseDto);
                return dtos;
            } else {
                return null;
            }
        } catch (IOException e) {
            e.printStackTrace();
        } catch (JSONException e) {
            throw new RuntimeException(e);
        }
        return null;
    }
*/

//}
